<%= form_for(sessionyear) do |f| %>
  <% if sessionyear.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sessionyear.errors.count, "error") %> prohibited this sessionyear from being saved:</h2>

      <ul>
      <% sessionyear.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :theme %>
    <%= f.text_field :theme, class:"form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :start_date %>
    <%= f.datetime_field :start_date, class:"form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :end_date %>
    <%= f.datetime_field :end_date, class:"form-control" %>
  </div>

  <%= link_to 'Cancel', sessionyears_path, class:"btn btn-secondary" %>
  <%= f.submit class:"btn btn-primary" %>
<% end %>
